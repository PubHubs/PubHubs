# configuration used by test using create_test_context()

database_location: null # use in-memory database
yivi:
  server_url: http://not-actually-used.example.com
pep: { }
allowed_embedding_contexts: ["localhost:8082,localhost:8080"]
admins: []
