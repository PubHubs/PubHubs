# Dependencies the hub imports directly or pins itself.
#
# Only list a package the hub imports itself, or one where the hub needs a higher floor than
# synapse.  matrix-synapse is pinned exactly, so synapse's own floors already arrive with its
# wheel metadata.
# ML-DSA (post-quantum) HHPP verification; >=48 ships ML-DSA via bundled OpenSSL 3.5 wheels
cryptography>=48.0.0
# cryptography>=49 dropped the _lib.GEN_EMAIL binding that older pyOpenSSL referenced at
# import; pin pyOpenSSL forward to match.  Keep in sync with the floor in pubhubs_hub/Dockerfile.
pyOpenSSL>=26.3.0
twisted>=24.11.0
livekit-api>=1.0.2
# Core.py uses nacl.secret.Aead (XChaCha20-Poly1305), which PyNaCl added in 1.5.0; synapse only
# constrains pynacl>=1.3 (transitively, via signedjson), which is too low for us.
pynacl>=1.5.0
# Use the same version as the docker image is built from.  The [oidc] extra pulls in authlib,
# needed by pseudonyms.py's `synapse.handlers.oidc` import; taking it from the extra leaves that floor in synapse's hands.
matrix-synapse[oidc]==1.160.0
