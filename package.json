{
  "name": "pubhubs-canonical",
  "version": "3.2.0",
  "private": true,
  "description": "PubHubs monorepo",
  "scripts": {
    "prepare": "husky",
    "install:global": "cd global-client && npm install",
    "install:hub": "cd hub-client && npm install",
    "postinstall": "run-p install:*"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
